user:
  name: otazuki
  github: otazuki004

language: "html"

commands:
  build:
    - "echo 'Building static site...'"
  package:
    - "echo 'Packaging static assets...'"
  start:
    - "echo 'No server-side code to start. Serve index.html'"

env:
  - "ENVIRONMENT=production"
